var fruit = 'orange';

var label;

if (fruit.length > 5) {
    label = 'The fruit name has more than five characters.';
} else {
    label = 'The fruit name has five characters or less.';
}

console.log(label);
